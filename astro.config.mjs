// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
	// Repo GitHub cible : SimplyyMarket/simplyymarket.github.io (site de profil,
	// donc pas de `base` à définir — voir docs.astro.build/en/guides/deploy/github/)
	site: 'https://simplyymarket.github.io',
});
